PORT=3000
DB_HOST=grupofmo.com
DB_PORT=3306
DB_USER=usuario_daw
DB_PASSWORD=tu_clave_aqui
DB_NAME=daw_grupo4

# JWT (Fundamentos JWT / Ejemplo practico — nunca subir secretos reales a Git)
# Genera un secreto largo, por ejemplo:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=cambia_esto_por_un_secreto_largo_minimo_32_caracteres
JWT_EXPIRES_IN=24h

# Usuario demo opcional (seed al arrancar si no existe)
ADMIN_EMAIL=admin@espam.edu
ADMIN_PASSWORD=TuClaveSegura123
